class IMUTest { 
    public:     
        bool gr1(bool checker);
        bool gr2(bool checker);
        bool gr3(bool checker);
        bool gr4(bool checker);
        bool gr5(bool checker);
        bool gl1(bool checker);
        bool gl2(bool checker);
        bool gl3(bool checker);
        bool gl4(bool checker);
        bool gl5(bool checker);
        void setup();
        void legPosition();
    private:
        /*
        //Are these two pins necessary for the IMU to work or is it all set?
        //Definitly run a couple of tests to check and see if this code will work.
        int IMU1Pin = A0;
        int IMU2Pin = A1;
        */
};
